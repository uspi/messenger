<Window x:Class="WPFClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Turing v1.0" WindowStartupLocation="CenterScreen"
        Icon="/Properties/wpfmessengerv2.ico" 
        Height="550" Width="450" MaxHeight="850" MaxWidth="800" MinHeight="450" MinWidth="400" 
        Background="#222" Foreground="#f6f6f6">
    <!--import styles from file ResMainWin.xaml-->
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ResMainWin.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid x:Name="applicationGrid">
        
        <Border Name="LoginScreen" Style="{StaticResource Screen}" Visibility="Visible" Background="#007e8f">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="365*"/>
                    <RowDefinition Height="154*"/>
                </Grid.RowDefinitions>
                
                <StackPanel Style="{StaticResource LoginPanel}" Height="210" Margin="0,140,0,15">
                    <TextBlock x:Name="Login" Text="Login" Style="{StaticResource HeaderBlock}" />
                    <TextBox Style="{StaticResource TextBoxBase}" Name="LoginBox" Initialized="LoginBox_Initialized" />
                    <PasswordBox Style="{StaticResource TextBoxBase}" Name="PasswordBox" Initialized="PasswordBox_Initialized"/>
                    <Button Content="Confirm" Style="{StaticResource ButtonBase}" Name="LoginButton" IsDefault="True" Click="ConfirmButton_Click"/>
                    <TextBlock Text="" Name="LoginMessageBlock" Style="{StaticResource WarningBlock}" Visibility="Hidden"/>
                </StackPanel>

                <Canvas Grid.Row="0"/>

                <TextBox Grid.Row="1" x:Name="DevLine" 
                         Text="Every thing ok!" Visibility="Hidden"/>
            </Grid>
        </Border>

        <Border Name="ChatScreen" Style="{StaticResource Screen}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50" />
                    <RowDefinition />
                    <RowDefinition Height="50"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Style="{StaticResource HeaderBorder}">
                    <Grid>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="6*" />
                            <ColumnDefinition Width="1*" />
                        </Grid.ColumnDefinitions>

                        <!--<Button Style="{StaticResource NavButton}" Grid.Column="0" Name="BackButton" Content="←" Click="BackButton_Click"/>-->

                        <TextBlock Style="{StaticResource HeaderBlock}" Text="" VerticalAlignment="Center" Name="ChatName" Grid.Column="1"/>
                    </Grid>

                </Border>

                <Border Grid.Row="1">
                    <ListBox Name="MessagesList" ScrollViewer.VerticalScrollBarVisibility="Auto" 
                             ScrollViewer.CanContentScroll="True" 
							 Style="{StaticResource ContactsList}" Focusable="False"
							 HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <ListBoxItem>
                                    <Border Style="{StaticResource MessageBorder}" HorizontalAlignment="{Binding Alignment}">
                                        <StackPanel Orientation="Vertical">
                                            <TextBlock Text="{Binding Text}" Style="{StaticResource MessageText}"/>
                                            <TextBlock Text="{Binding Date}" Style="{StaticResource MessageDate}"/>
                                        </StackPanel>
                                    </Border>
                                </ListBoxItem>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Border>

                <Border Grid.Row="2" Style="{StaticResource HeaderBorder}">
                    <Grid>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="6*" />
                            <ColumnDefinition Width="1*" />
                        </Grid.ColumnDefinitions>

                        <TextBox Name="MessageBox" Style="{StaticResource MessageBox}" Grid.Column="0"/>

                        <Button Style="{StaticResource NavButton}" Grid.Column="1" Name="SendButton" Content="→" Click="SendButton_Click" />
                    </Grid>
                </Border>
            </Grid>
        </Border>

        
        <Border Name="ContactsScreen" Style="{StaticResource Screen}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50" />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Style="{StaticResource HeaderBorder}">
                    <TextBlock Style="{StaticResource HeaderBlock}" Text="Contacts" VerticalAlignment="Center"/>
                </Border>

                <Border Grid.Row="1">
                    <ListBox ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.CanContentScroll="True" 
								Style="{StaticResource ContactsList}" Name="ContactsList" SelectionChanged="ContactsList_SelectionChanged">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <ListBoxItem>
                                    <DockPanel LastChildFill="True">
                                        <Image Style="{StaticResource ContactImage}" DockPanel.Dock="Left"></Image>
                                        <TextBlock Text="{Binding Name}" Style="{StaticResource ContactName}" DockPanel.Dock="Right"/>
                                    </DockPanel>
                                </ListBoxItem>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Border>

            </Grid>

        </Border>
    </Grid>

</Window>
